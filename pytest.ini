[pytest]
testpaths = tests
# The repo lives in a OneDrive folder whose reparse points break pytest's cache
# directory creation (WinError 2). Disable the cache provider to avoid the warning.
addopts = -p no:cacheprovider
